node_modules
dist
dist-ssr
*.local

# 编辑器 / 系统
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
.codebuddy/

# 浏览器自动化（playwright-cli 的快照 / 控制台日志等临时产物；
# 用 `playwright-cli` 重新截图时会再生成。README 用的截图在 screenshots/，要提交）
.playwright-cli/

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 构建期产物（S1.5 数据核对报告，可随时重跑生成）
reports/

# 本地环境与验证缓存
.env
.env.*
!.env.example
coverage/
*.tsbuildinfo
*.log
.worklog/

# 旧原型及历史数据；当前应用使用 src/db/
/data/
/prototype/

# 本机部署脚本（含服务器上的绝对路径，不随仓库分发）
/update.sh
