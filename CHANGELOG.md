# 变更记录

> 本文件记录 yys-checklist 的所有变更，是改动的唯一流水入口。
>
> - 格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
> - 版本号与 `package.json` 的 `version`、`src/db/meta.db.json` 的 `meta.version` 保持一致（当前 `1.4.0`），本文件只引用不发明。
> - 分组固定五类：**新增** / **修改** / **修复** / **移除** / **数据**（数据 = 条目库、枚举、数据版本号）。
> - 日常改动只追加到 `[Unreleased]`；发版时把该区整体归入新的版本小节并补日期，`[Unreleased]` 重新置空。
> - 仓库暂无 git tag，因此不写版本比较链接。
> - 每条一行，只写「改了什么」，不预设动机、不复制 `docs/` 正文；待办与未修复项记在 `docs/04-handover-guide.md` 的路线图一节，不写进本文件的修复分组。

## [Unreleased]

### 新增

- **册页重设计批 5+6 落地**（用户确认"交互逻辑不改，样式可以改"后继续；参考稿 `uiRef/囤囤鼠大作战_重设计样式稿.html` 的余下模块）——
  ① **汇总条** `components/common/SummaryBar.tsx`（册页稿 `.summary`）：待办 N · 已成 N · 金线进度轨 · 各页口径小字，接在四个清单页的页头之下 —— **页级进度只在这一处**，分组头的 2px 进度底轨随之退场（`SectionTitle.progress` 参数保留不再渲染）；
  ② **桌面右列收益**（册页稿 `.entry .pay`）：`GainBadges` / `KindBadges` 增加 `column` 形态（标签在上、徽章右对齐），`ChecklistItem` / `ChecklistGroupCard` 按 `useBreakpoint` 分流 —— 桌面独占右列成一条竖线、移动端留在正文流，桌面说明文字不再因徽章换行提前折行；
  ③ **统计页重排**：日历换 `.panel` 外壳 + `.panel-head`（衬线题名 + 渐隐线）、格换纸片形（今日朱红描边 + 12px 朱红短线、格内第二行显示当天完成数）；收益三卡换 `.gain`（币种点标签 + 衬线大数字）并新增 **spark 逐日细柱** —— 数据直接用 `summarizeRangeGain` 已返回的 `byDay`，不算第二遍账；区间切换换 `.seg` 分段控件（选中转按钮红）；空态换 `.hint`、口径脚注换 `.foot`；
  ④ **分组头改排**（`.grp-head`）：组名由金字改回墨色衬线、虚线改渐隐实线、计数 pill 改裸等宽计数；
  ⑤ **墨线菱点 `Rule`** 与**收束纹带 `Closing`**（四字铭：一日一愿 / 七日一折 / 一页一月 / 到期即归档）替换四页页脚；`KikyoBand` / `Shimenawa` 与 `--c-band`、`.genso-band-line` 随之全库无调用点，按"死符号要么用上、要么删"退场；
  ⑥ **设置行改排**（`.srow`）：`SettingRow` 由阴影浮起卡改描边平贴、标题回正体、行距 8px；`DataVersionSection` 的明细换 `.dlist`（键定宽 132px 档、值等宽）；
  ⑦ **工具页寄养表单改排**（`.form` / `.frow` / `.inp`）：填充底外壳 + 纸底等宽输入框 + 定宽行标签，提交按钮走 `controls.btn.sec` 配方。全部为渲染层改动：store、domain、事件处理一行未动
- **「回到顶部」朱印按钮**（用户要求："为每个页面加上，滚动到非最顶部时出现，在顶部时不用出现"；参考稿 `uiRef/囤囤鼠大作战_回到顶部按钮.html`）：**挂在两端壳层**（`MobileShell` / `DesktopShell` 的内容区各包一层 `relative` + `BackToTop`），所以七个页面自动都有 —— 它不是页面级控件。按钮**锚在滚动容器（`main`）那一层**而不是 `fixed` 到视口：它的"底"就是底栏的顶（移动端 `bottom-2` = 比底栏高 8px，取自参考稿集成场景量过的值；PC `right-7/bottom-7` 取自该稿 `.rt-fixed`），于是底栏高度、iOS 安全区怎么变都不用改魔数。图形为新增的 `ascend`「昇雲箭」（`sprite.tsx` C1，全库 45 枚）：云拱托起一支箭 —— 三条要害记在注释里（**云拱是一条对称贝塞尔**而非波浪拼贴，17px 下才是一条干净的拱；箭羽末端 15.2 与云脊 15.5 留 **0.3 空隙**，箭是"坐在云上"不是"插进云里"，差这 0.3 气质从「落」变回「昇」；云脚**不封口**，一封口就读成"倒置的伞"）。样式在 `theme.css`：新增 `--rt-ink`（朱底亮金）/ `--rt-ring`（金圈，要 alpha 故存完整 rgba）两个令牌 + `.genso-rt` 组件类（正圆朱红径向渐变 + 1px 内金圈 + 悬停外金环 `::after` 外扩 5px **不撑盒模型** + 图标占 56%）。**行为按用户口径只两态**：滚动量 > 4px 显示、回到顶部加 `.is-hidden`（淡出下沉 + `pointer-events:none` + `tabIndex=-1` + `aria-hidden` —— "看不见却能被 Tab 命中"是陷阱，不只是视觉问题）；参考稿另画的「未达阈值 320px 才浮出」与「到顶态淡到 28% 留位不重排」**没做**，理由与改法写在组件注释里。无障碍按该稿那 8 条落地：真 `<button>` + `aria-label`/`title` 双写 + 图标 `aria-hidden` + 命中区 44×44；`scrollTo` 的 smooth **自己判 `prefers-reduced-motion`**（全局那条 CSS 兜底只管 transition / animation，管不到 JS 滚动）；点完后若焦点仍在按钮上会主动 blur（那时它已隐藏）。**三处刻意不照抄参考稿**：① 渐变三端复用 `crimson-hi → crimson → crimson-deep`，不用它的 `#C93C38 / #B7232A / #8A161C` —— 那组值出自**上一代（方正）色阶**（同稿 `--crimson:#B7232A` 亦然），照抄会在库里立起第二支朱红；② 亮金的对比度按本项目朱红重算（明 **4.40:1** / 暗 **3.95:1**，图形门槛 3:1 有余量），参考稿标的 4.9:1 是对它自己的底色算的；③ 焦点环不自己写 —— 全站唯一那支 2px 金环在 `base.css`，只差参考稿的偏移量（4 → 2）。`docs/01` 的组件清单与壳层说明已同步
- 新增 `docs/05-icon-inventory.md`（图标槽位清单，重设计底稿）：用户提出「有些 icon 和对应中文没什么关系，要重新设计一套」，本文把**每个槽位的语义**逐处列出——36 枚符号、约 90 处引用、30 余个文件，按「品牌与导航 / 周期符与分组头 / 清单卡字段 / 时间与状态 / 操作按钮」五组列表，逐行标注 ✓可读 / 一般（靠约定）/ ✗名不副实，并给出三处系统性诊断（**一枚符号担多义**：`torii` = 今日页 + 每日条目 + 入口字段 + 空态、`koyomi` = 本月页 + 月常 + 版本赛季；**三枚回转箭头难分**：`restore` / `undo` / `refresh` 分别表示恢复 / 撤销 / 刷新；**通用 UI 隐喻不必和风化**）。文末给出重设计分组（周期 / 页面 / 字段 / 状态 / 操作）与两种改动口径（只重画 → 改动锁在 `sprite.tsx` 一个文件；要拆义则连同 `IconName` / `NAV_ICON` / `FIELD` / 两处 `CYCLE_ICON` 一起改）。同时点出 2 枚**死符号**（`shimenawa` / `sensu`，无调用点）与无人调用的 `ornament.StatStrip`。文档索引 `docs/README.md` 已登记
- 设置页新增「**清空记录**」危险分区（用户要求）：**清空今天** / **清空全部**，各带二次确认，排在「数据」组末位。清空范围**只含勾选与日志** —— 账号、偏好、自建条目、寮时间各有自己的重置入口，不跟着一起没。**「清空今天」以日志为事实来源**（`log.days[今天]` 正是"今天勾了什么"的历史面），把当天 id 交给 `setMany(ids, null)`，日志沿既有的「取消勾选按周期起点回退」规则自行收敛 —— **零新增状态逻辑**，新功能只是既有原语的一次组合；没去猜 `checked` 的时间戳窗口，以免为"今天从几点算起"维护第二份真相（`resetHour` 与自然日是两个口径）。两颗按钮都用**细朱红描边**（`btn.danger`）而不是实心朱红渐变：后者在本项目是"主行动"的造型，拿它装"清空全部"等于把破坏性动作包装成推荐动作 —— 权重交给确认弹窗那句"清掉多少项、影响哪些页面"。分区走 `CollapsibleSection` + `tone="danger"`（该分支本就是为"清空勾选"预留的）：默认收起防误触，朱红边框让它在扫描时可辨。`domain/checkLog` 新增 `idsLoggedOn`（带 2 条单测，钉住"清空当天不会抹掉上个周期的记录"）
- **按次数聚合的进度卡**（用户需求，方案 B：**数据一行不改，只在渲染时聚合**）：`地域鬼王 1/3 · 2/3 · 3/3` 这类"同一件事的第 k / N 次"在列表里折成**一张卡** —— 菱形当**推进器**（点一下完成下一步，满段后再点 = 取消整组），标题行显示 `cur/total` 等宽计数，卡片底轨显示真实进度。**进度 = 组内真实已完成的条数**，不是另记一个计数，所以 2026-09-20「常驻条目按次数拆分」留下的逐次说明与收益全部保留（`2/3` 那条的"需声望 2000"只在推进到那一步时显示）。之所以不走"数据并成一条 + `times` 字段"：那要改**已发布的数据形状**（本地分片 + 备份 + 日志 + 统计口径），而且逐次说明要么被合并（有损）要么塞进同一张卡（等于又要分段）
- 新增 `domain/grouping`（纯函数 + 单测）：聚合依据只有名字里的 `k/N` 后缀与 `cycle` + `path` 是否全同，且要求 `k` 恰好凑齐 `1..N`；**任一条件不满足就整堆退回单条**（错聚合比不聚合糟得多）。名字才是"用户看到的分组依据"，所以解析用名字而不是 id ⚠️ **"凑齐 `1..N`"这条已于同日修订掉**（缺员按可见步数聚合），见本区「修改」组第一条
- **聚合必须在「待做 + 已完成」的并集上做**（`groupChecklist`，2026-09-24 用户报"地域鬼王怎么没聚合"后补）：清单页是两段渲染，很自然会写成 `groupByCount(pending)` + `groupByCount(done)`，但**只要勾掉 3 次里的任何一次，那组就被拆到两个数组里，两边都凑不齐 `1..N`**，于是整堆退回单条 —— 表现就是"一勾就散"。现在先并起来聚合，再按"整组是否全部完成"切回两段：做了一半的组留在待做区（带 `cur/total` 进度），全完成才进已完成区。这条已写进 `grouping.ts` 的文件头，别再分开算
- 新增 `components/common/ChecklistGroupCard`（聚合卡）与 `ChecklistEntry`（单条/组的分发件）；四个清单页各只改一行映射（`xxx.map((item) => <ChecklistItem/>)` → `groupByCount(xxx).map((u) => <ChecklistEntry/>)`）
- 聚合卡的交互三处定档（可按反馈调整）：① **点整卡 = 点菱形 = 推进 1 步**，不设"一键完成整组"（否则整卡一点就满段，分段推进就失去意义）；② **满段后再点 = 取消整组**；③ **长按 = 把当前进度复刻到其他账号** —— `toggleInProfiles` 的方向由第一个 id 的状态决定，所以传参把"下一步"放第一个（它必然未完成 → 整批按勾选写），目标账号拿到的正好是"前 cur+1 步已完成"；满段时长按传整组（首个已完成 → 按取消写）
- 聚合卡的"宁可少显示"原则：逐次不同的信息（时间窗 / 收益 / 奖励类型 / 入口）**只在全组一致时才显示**，否则整项不显示（把 `1/3` 的信息当成 `2/3` 的显示比不显示更糟）；唯一例外是**截止**取全组最早（临期提示宁可早不可晚）。逐次说明（`note` / `condition`）不受此限 —— 它显示的就是当前这一步的原文
- 聚合卡**不提供置顶星标**：置顶是"每条"的属性，组内成员各自置顶会在同一位置打架；也不自动占用「唯一高亮位」（该位仍由页面决定，限时页仍给临期条目）

- 朱印视觉体系（落地 `uiRef/` 的设计规格）：`src/styles/theme.css` 新增明暗双主题 CSS 变量（结构色 / 四态色 / 币种三色 / 麻叶纹与点阵 data-URI），`tailwind.config.ts` 的颜色槽改为 `rgb(var(--c-x) / <alpha-value>)`，并新增 `gold` / `gold-hi` / `gold-soft` / `crimson*` / `card-done` / `surface-deep` / `state-*` / `track` / `scrim` 令牌与 `font-serif` / `font-mono` / `tracking-*` / `shadow-panel|card|ready` / `ease-genso` / `animate-reveal`
- 明暗双主题（设备级）：`stores/theme.ts` 与 `DEVICE_KEY.theme`（`yys:theme`），初始值优先本机存档、其次跟随 `prefers-color-scheme`、都读不到才落回明版；用户未显式选择前持续跟随系统（`App` 订阅 `matchMedia`），`main.tsx` 在渲染前写入 `data-theme` 以免暗版用户先看到一帧明版。设置页新增「观感」分区（明版 / 暗版分段控件），两端壳层各挂一个切换入口（`components/common/ThemeToggle.tsx`）
- 自建和风图标库：`components/icons/sprite.tsx`（36 枚 `<symbol>`，统一 `viewBox 24×24`、`stroke-width 1.5`、无底板无外框）+ `components/icons/Icon.tsx` + `components/icons/navIcons.ts`；`IconSprite` 在 `App` 顶层挂一次
- 工艺层零件库 `components/ornament/`：麻叶纹与细点阵底纹、内框 + 四角折角、御币、桔梗纹带、注连绳、蛇目纹、达摩点睛（全清时点亮右眼并由金转朱红）、竖排书脊、右缘御灵符与「封印」、面板外壳、`StatStrip` 统计三卡
- `components/common/PageHead.tsx`：清单页共用的页头（衬线标题 + 全页唯一的等宽倒计时）

- 交接文档体系：`AGENTS.md`（红线与命令速查）、`docs/README.md`（文档索引与更新约定）、`docs/01-architecture.md`（架构与渲染链路）、`docs/02-data-and-domain.md`（数据层与领域逻辑）、`docs/03-conventions-and-tests.md`（强制约定、测试与工具链）、`docs/04-handover-guide.md`（上手步骤与改动任务手册）
- 「本月」页：月常条目从「本周」页拆出、独立成一级页面（导航 7 项：今日 / 本周 / 本月 / 限时 / 统计 / 工具 / 我的）—— 两者刷新口径不同（周一 0 点 vs 每月 1 日 0 点）。`useChecklist` 新增 `'month'` 目标（`'week'` 不再含月常），`domain/dateLabel` 新增 `monthRangeLabel`（含单测）；移动端底部 Tab 由 `grid-cols-6` 改为 `grid-cols-7`，桌面侧栏补 `CalendarDays` 图标，`NavContent` 增加分派分支
- 勾选日志 `CheckLog`：新增 `yys:checklog:{profileId}` 分片，按 `YYYY-MM-DD` 分桶记录当日勾选的条目（幂等、保留 90 天），**周期重置不清它**，只有取消勾选才按该条目的周期起点回退（`domain/checkLog` + `stores/check.logAfter`，均带单测）。它是"哪天做过什么"的唯一来源 —— `checked` 只留每条最近一次。随备份导出 / 导入（`UserDataBundle.data[].log`；旧备份缺该字段时归一成空日志），契约新增 `saveCheckLog`（31 个方法），`getBootstrap` 的 payload 增加 `log`
- 统计页整版改版（`pages/StatsPage.tsx`）：上方「月度日历」按**当月单日最大值**相对分档着色、点某天即选中；下方「时间区间收益」为 近 7 天（默认）/ 近 30 天 / 本月，点日历切成单日、再点一次（或按「返回近 7 天」）回到快捷区间。区间收益走新增的 `domain/stats.summarizeRangeGain`（输入是勾选日志 —— 同一条目多天完成即累计多次，因此无「总量 / 还差」），日历排版走新增的 `domain/calendar`（周一起始、固定 6 行、含前后补位格）。原「本日 / 本周 / 本月进度条」与 `GainBar` / `ProgressBar` 保留待恢复但已无消费方

- 今日 / 本周 / 本月三页顶部加**周期倒计时**（精确到天和小时）：`domain/reset` 新增 `periodEndOf` —— 它与 `periodStartOf` **共用同一组锚点函数**，所以倒计时归零的那一刻正好就是勾选被重置的那一刻；若另写一套"月末 24 点"之类的算法，两者迟早会在某个边界（跨月、`resetHour` 非 0 点）差出一档，而用户会先看到倒计时归零、再发现勾选没被重置，比没有倒计时更糟。`domain/countdown` 新增 `formatRemain`（天 + 小时，`floor` 口径，不足 1 小时说"不足 1 小时"而非"剩 0 小时"），`hooks/usePeriodCountdown` 每分钟重算。本月页原有的「每月 1 日 0 点刷新」保留 —— 规则与倒计时并列，既说清"什么时候刷"也说清"还有多久"。单测里有一条"两个函数必须闭合"的用例把这个不变量钉住

### 修改

- **册页重设计第一批落地**（参考稿 `uiRef/囤囤鼠大作战_重设计样式稿.html` + `囤囤鼠大作战_移动端样式稿.html`；评估与用户决议记录在 `uiRef/册页重设计_落地评估.md` §六）：**令牌层 + 两端导航 + 清单卡→账目行**。① 令牌照稿全套重算：明 `ink 35 32 28` / `gold-hi 125 93 26` / 暗 `gold-soft 42 38 32` 等，新增 `gold-line`、`crimson-btn(-hi)`、`seal(-hi)` 六个令牌 —— 其中**按钮红与主题朱红分家**（暗版主题朱红配白字仅 2.86:1，按钮另取深一档 → 5.35:1，参考稿算过）；底纹换成 **84×84 菱形网格纹**（单一 `--tex`，麻叶纹+点阵退役）；**删掉暗角层**（`body::after` + `--mask`，参考稿没有）；缓动换 `cubic-bezier(.22,.61,.36,1)`；等宽字体 JetBrains Mono 提到首位；新增 `text-3xl`(29px) 档；**圆角档位保留未动**（与参考档差在 2px 内，换档会牵动 40 处组件且不可视觉验证）。② 桌面导航：**侧栏 → 顶栏索引签**（选中签 `bg-surface` + `-mb-px` 与册页相连、签顶朱线、←→ 键盘导航），账号切换/结界倒计时迁至顶栏右端（决议 4），`OfudaDeco` 御灵符装饰随侧栏删除（决议 2），新增品牌印组件 `ornament.Sigil`（`.genso-sigil` 径向渐变走 CSS 类豁免）。③ 移动导航：应用栏收成 **56px 单行**——品牌印 + 当前页名 + 眉批（则①，眉批为静态定位语、动态计数仍在页内），右端三件套常驻（决议 4）；底部 Tab 保持 7 格（决议 3），选中色由金改朱红。④ `PageHead` 两副面孔（四个页面调用点零改动）：桌面 = 书眉（29px 大标题 + 右侧倒计时），移动 = **金券条**（用途 · 等宽大字 · 绝对时刻）。⑤ **清单卡 → 账目行**：`ChecklistItem` / `ChecklistGroupCard` / `HubCard` 由卡片改为行式（行铺在册页上、行间细分隔线、hover 铺填充底、左缘朱线标进行中），`CHECKLIST_GRID` 双列退役改通栏单列（决议 5，2026-09-11 的双列决策随之作废）；勾选框缩小一档（20 → 18px，用户要求）；任务名转正体 13px；奖励徽章 **chip 化**（币种点 + 11px 一族）；主按钮渐变换 `crimson-btn` 档且去描边。⑥ 文案跟稿：已完成 → **今天已结 / 本周已结 / 本月已结 / 限时已结**。**决议否决项**：点行展开（meta 显隐交给既有疏密度设置）、勾选框退役（保留）、5 Tab +「更多」（Tab 仍 7 个）。**下一批未落地**：汇总条、收益三卡+spark、统计日历重排、工具页表单纵向堆叠、墨线菱点分隔的全面替换
- **图标集适配参考稿同日追加的「⑧ 本轮复审」（用户要求"刚刚更新了，适配一下"）**：参考稿对上一版**未动的 44 枚**逐枚量化（按「24 视野 + 半线宽」算包围盒查越界 / 算中心到 (12,12) 的距离查偏心 / 同尺度光栅化比剪影 IoU 查撞图），16 枚问题本项目落地 **15 枚**（第 16 枚 `sensu` 上一轮已删）。改动**全是 `<path>` 替换** —— `IconName` 联合与约 90 处调用点零改动，改动面锁在 `sprite.tsx` 一个文件（正是 `docs/05` 说的"只重画、不改名"那条路）。逐枚：**P0 ×5** `seimei` 由「空心五角星」改为真·五芒星一笔连星（原与 `star5` 同族剪影，一张图分不出「线 / 面」）、`ema` 与 `koyomi` 的形对调（上一版两枚剪影重合度 0.453，在周期组里互相冒充）、`daruma` 去掉半径 2.1 的环眼重画（原读成鬼脸 / 骷髅）、`ofuda` 符尾由 y=24.2 收进 21.6（**全集唯一越出 24 视野、17px 下被裁成平口的一枚**）；**P1 ×9** `ougi`（重心下坠 2.4 为全集最重）、`chart`（2.2）、`suzu`（高悬 1.8）、`kanazuchi` / `hako` / `chochin`（0.9~1.2）各按位移量整体归位，`torii` 去掉顶部多余竖线并把额束移回岛木与贯之间，`shimenawa` 波浪由 0.8–23.2 收进 1.4–22.6（原左右各越界 0.05，横幅拉伸时两端被切），`refresh` 环口由 26° 开到 80°（与「环勾」剪影重合度 0.593 → 0.463，工具区并排不再互相冒充）；**P2 ×1** `kasane` 右移 0.7 回到左右对称。另把复审里**两类被量化指标误报、经人眼复核确认无需改**的形写进 `sprite.tsx` 文件头（实心星 / 菱的墨迹质心天生顶到上缘；三角 / 鸟居 / 柱状的质心天生偏向横画）—— 免得下次有人照指标去"修"而把整族带歪。`docs/05-icon-inventory.md` 的「已定义但无人使用」由 2 枚改为 1 枚：`sensu` 已删，`shimenawa` 形状修好但仍无调用点。**未做**：参考稿 ⑧ 末尾提到姊妹稿 `阴阳师任务录_朱印深化版.html` 内嵌 9 枚更早的副本（几何更旧、且仍用晴明纹充当「设置」页签），参考稿本身也把"要不要一起翻新"留给了用户，故未动
- **落地 UI 审查意见的 A 类（`uiRef/囤囤鼠大作战_UI审查意见.md`，先做无争议部分）**：全是可复算的对比度修正，不动结构与版式 —— ① `ink-3` 两个色值：明 `125 116 102` → `112 103 89`（**5.21:1**）、暗 `138 132 150` → `152 147 166`（**5.26:1**），基准均为真实卡片底（`#FCF7ED` / `#23222F`）；② 明版 `ink-3` 注释改正：原写"白卡上 4.6:1"，按纯白确实是 4.60，**但真实卡片上只有 4.31** —— 基准色错一位就把"差一点"判成了达标，教训（注释里的对比度必须标基准色、基准色要用真实面）写进了令牌注释；③ **文字金一律走 `gold-hi`**（明版 `gold` 作文字仅 2.94，`gold-hi` 4.64）：`ChecklistItem` / `ChecklistGroupCard`（未完成侧去掉 `opacity-85`）、`EmptyState`（组头图标 + 标题 + 计数 pill）、`CollapsibleSection`、`Tags` ×2、`GainBadges`、`ProfileSection`、`ProfilePickDialog`、`OnboardingDialog`、`ThemeToggle` ×2（hover 由 `text-gold-hi` 改为 `text-ink`，保住"悬停更实"的方向）、`TodayPage`、`YuhunSection` ×2、`controls.sec` / `controls.gold`、`ChecklistItem` 置顶按钮 hover —— `gold` 从此只留给描边与纹样（`ornament` 的 4 处是 `aria-hidden` + `pointer-events-none` 的装饰，对比度豁免，保留原样）；④ `ink-4` 越界当正文的 3 处提到 `ink-3`：**删档前的风险说明**（`ClearDataSection`，最要紧的一处）、御魂段的跟随小字与页脚口径（`YuhunSection`）；⑤ **已完成项的三级衰减拆掉一级**：删去整卡 `opacity-60`（`ChecklistItem` / `ChecklistGroupCard` / `HubCard`）——（卡底色 × 整卡 60% × 文字 50%）实测只有 **1.68:1**（暗 1.83），已接近"消失"，与既定口径「不消失、沉下去」相悖；同时把已完成**任务名**与类型符由 `text-ink-4`（含 `opacity-50`）提到 `text-ink-3`，沉下去仍由卡底色 `card-done` + 朱红划线 + 菱形符三个通道承担。⚠️ 审查清单只列了"类型符"那一处，**任务名那三处与其余 10 处残留 `text-gold` 是核查时补的**（任务名才是读者最需要看清的字）。该审查经逐条核查：13 项中 **11 项成立**（A1 / A2 / A4 / A5 的对比度与 ΔL\* 我手算复现一致），`B1` 的"工具页与统计页已用 `Segmented`"、`C4` 的归页两处依据有误。**未做（都有取舍，待定）**：A5 明版 `card-done` 再降一档（审查推荐 `#E9E0CE`、倾向 `#EDE5D5`）、B1 今日页页签（保留符札签形 vs 换胶囊）、C1 补页头（**设置页那条与 2026-09-23 用户要求的"页头整块去掉"冲突**）、C2 三页空区、C3 内容区上限与第三列断点、C4 行长收束、C5 移动端 10px 字、E1 `s-locked` 令牌二选一。另：审查把 `StatsPage` 的**非本月日期**算作 `ink-4` 越界，核查后**保留** —— 那是弱化态，正是 `ink-4` 的既定用途
- 图标集按参考稿重设计并落地（`uiRef/囤囤鼠大作战_和风图标集.html`，用户要求"参考进行修改"）：`sprite.tsx` 的 `<defs>` **整段替换**（42 枚符号，按参考的 A1/A2/A3/B1/B2/C1/C2 分组），`IconName` 联合与全部调用点同步。**7 枚新增**：`ema` 绘马（每日 / 今日页）、`nobori` 幟（版本 / 赛季）、`chart` 柱状（统计）、`setting` 双滑杆（设置）、`makimono` 卷物（数据快照）、`done` 环勾（已完成）、`pin` 图钉（置顶）；**3 枚重画**：`star5` / `rhomb` 统一到 24 坐标系（原用微型 viewBox，经 `<use>` 拟合后缩放与其余符号不一致）、`restore` 改双环（旧版与「刷新」互为镜像、扫一眼分不开）、`kanazuchi` 收细；**1 枚改派**：`suzu` 神乐铃 → 一键日常入口卡（原与「工具」抢金槌）；**1 枚删除**：`sensu` 团扇（那个分区早已不存在）。参考的 **9 处「旧→新」全部落地**：每日/今日页 `torii → ema`（含两处 `CYCLE_ICON`、今日页 Tab 与组头）、统计 `seimei → chart`（五芒星不能既当 logo 又当功能图标）、设置 `daruma → setting`、已完成 `suzu → done`（今日/本周/本月/限时四个页）、置顶 `star5 → pin`、数据快照 `fumi → makimono`、一键日常 `kanazuchi → suzu`、清空配置 `restore → trash`（"清空"原用了"恢复"的图，语义相反）、恢复 `restore` 换新画法。另按参考①把「版本 / 赛季」由 `chochin` 改 `nobori`（两处 `CYCLE_ICON` 与限时页该分区组头）。`torii` 收敛为「入口」单一含义（空态大图仍用它）。`daruma`（收窄为"达成"）与 `star5`（稀有度）目前无生产消费方 —— 达摩点睛走的是 `ornament` 的可变宽画法，非本符号。`shimenawa` 由"死符号"转为保留待用（页内收束横幅仍是 `ornament` 的可拉伸内联画法，两者用途不同）。**参考未覆盖、我自行判断的两处在此备案**：设置页「观感」组原用 `daruma`（达摩语义已收窄为"愿/达成"）→ 改 `sun`；「委托」组仍用 `torii`（未动）。`docs/05-icon-inventory.md` 顶部加注"已落地、现用图标一列已过期"，保留作改版前的诊断记录
- 御魂速查表按用户反馈改两点（同日前一版的修订）：① **分段键由"类别"改为"两件套效果原文"** —— 上一版把「暴击 15%」与「暴击伤害 20%」压在同一个「暴击」块里、靠块内子标题区分，用户要求**拆开**；现在两者各占一段（共 20 段），段标题即效果原文，段序仍取 `dicts.soulCategory` 的 sort。唯一例外是**首领御魂 13 条**：它们本就没有四件套、13 句 `effect2` 各不相同且都是长被动，拿哪句都当不了标题 → 合并为一段，标题用类别名「首领 · 无四件套」。② **四件套效果改为常驻**（用户要求：超长换行即可，不截断）—— 每条御魂现在是「御魂名 | 四件套原文」**两列**；首领御魂回落显示两件套原文（那就是它的全部机制，不留空白）。表格结构用真 `<table>`：**每个分类各一张表**（`<thead>` 段首行跨两列写该类的两件套效果原文），`colgroup` + `table-fixed` 定死名字列宽（`table-fixed` 的列宽取自首行，而首行是跨列单元格，不显式给宽度会退化成两列等宽）—— 首版曾改成"段标题 + 列表"，用户当天反馈"表格效果怎么没了"，遂改回真表；紧接着又反馈"不同分类的中间隔开"，故从"20 类共处一张表、只靠一条浅线分隔"改为**每类各自成卡 + 类间留白**，与上面"每系列一张表"同一套视觉语言。`note` 仍在名字的 `title` 里。**数据仍未改**（分段键、四件套、首领判据全部来自既有字段：`effect2` / `effect4` 缺省 / `category`）
- 御魂页底部新增「**御魂速查 · 按两件套效果**」（用户要求）：全部 70 条御魂按两件套效果归类，左列类别、右列该类御魂名。分类取**结构化字段** `SoulRow.category`，组序与标签取 `dicts.soulCategory` 的 `sort` / `label`（首领 → 攻击 → 生命 → 防御 → 暴击 → 效果抵抗 → 效果命中）—— **不解析 `effect2` 文本反推类别**（AGENTS 铁律 7：用正则从自由文本推断语义已被明令禁止，曾有 87 条误判回滚的记录）。组内再**按效果原文分一层** —— 类别只是粗分：「暴击」组里 10 条是「暴击 15%」、1 条是「暴击伤害 20%」（无刀取），首领类 13 条各一种效果。所以**分类键始终是效果本身**：该类别只有一种效果时原文写在左列，有多种时逐种单起一行（左列改标「N 种效果」，**只含一条的效果不再起标题，改为「名字 · 原文」同一行** —— 首领 13 条若各占"标题 + 名字"两行会白占一倍高度）；少了这一层就会把"暴击伤害"混进"暴击"，读的人会以为它们同属一个两件套。实测分组：攻击 11 / 防御 11 / 生命 11 / 效果命中 6 / 效果抵抗 7 各只有一种效果；暴击两种（`暴击 15%` 10 条 + `暴击伤害 20%` 1 条）；首领 13 条各一种。⚠️ 本条描述的"类别为一级、效果为二级"结构在同日下一条被替换为"直接按效果原文分段"四件套与备注进名字的 `title`（不占版面）。**数据未改**：这是既有字段的第二个视图
- 御魂页改为「**每个系列一张表**」（用户要求：只需知道哪天掉哪些御魂，其余不展示）：行 = 副本、列 = 周一…周日、格 = 当天名单（**竖排、不截断**），今天整列高亮（表头金底白字 + 列底淡金）。用 `table-fixed` 且**不横滑** —— 横滑会让"一周对比"失效（看不全就失去意义）；375px 屏预算：副本列 74px + 7 列各约 35px，`text-2xs`(10px) 正好放 2–3 字名，4 字名（如「日女巳时」）换两行，桌面档位字与副本列各放大一档。删掉：模式徽章、`sub` 解锁条件、`tip` / `dayTips` 文案、7 日条与"今日详情"的重复渲染（本是同一件事的两处）、二件套 chip 列表。**数据一行未改**（纯渲染层取舍）。三样"事实"保留并写明理由：① 跟随关系（业原火 `followId`、永生之海 `followOld`）—— 不写则那几格是残缺名单；② 特殊产出（真·八岐大蛇不产常规御魂，产出在 `reward`）—— 否则整行是「—」，像数据缺失；③ 二件套效果移进名字的 `title`（悬停可见，零版面成本）。`randNote` 降级为周末「随机」格的 `title`，页脚由三段长文收成一行（来源与排除名单保留）。`domain/yuhun` 未改：`MODE_LABEL` / `hasDayGrid` / `oldFollowInfo` 仍有单测覆盖，只是本页不再消费
- 修工具页·御魂段**移动端分组标题被挤断**（用户报"标题换行"）：分组头那一行是 `flex`（不换行）且标题没定宽，而分组备注长达 40 余字（「魂1-10 → 魂土(悲鸣) → 魂王(神罚) → 魂13·虚无 · 同源四本，四套轮换别记混」）—— 两个 flex 子项按比例收缩，**CJK 的最小内容宽度只有一个字宽**，标题于是被压成一列字，屏幕越窄断得越碎。改为 `flex-wrap` + 标题 `flex-none`（永不收缩、永不断行），放不下时整条备注换到下一行占满宽度；口径与统计页月历头（`flex flex-wrap justify-between`）一致。同页另两处 `flex` 行已核过：卡片标题行是 `h3 flex-1`（basis 0，只吃剩余空间，不会被徽章挤）、`SoulChip` 是「id `flex-none` + 效果 `min-w-0 truncate`」，两处都符合该口径，无需改动 —— ⚠️ 其中分组头与卡片、`SoulChip` 随后在同日的「每系列一张表」改版中**整块移除**（见上条），本条的修法已不再是现行结构，保留作为"这类挤断成因"的记录
- **修「地域鬼王怎么还是没有合并」（用户第二次反馈）+ 聚合卡补上菱形进度格**：上次只修了"已勾成员被算进已完成数组"，但**视图筛选在更早的 `useChecklist` 里就把成员滤掉了** —— 它压根到不了聚合函数，所以同一症状会以另一条路径复现（没动过的组不缺员，于是只有地域鬼王散着）。两层一起改：① **分组成员豁免视图筛选**（新增 `domain/grouping.groupedMemberIds`，`useChecklist` 用它把被「覆盖隐藏」/`days` 星期不适用、以及当时的「隐藏已完成」（同日整体删除，见「移除」组首条）滤掉的分组成员捞回数组，再由 `pending/done` 归属决定落在哪一段）—— 过滤器只能决定"这条自己显不显示"，不能决定"这个分组存不存在"；② **聚合不再要求 `k` 凑齐 `1..N`**（`grouping` 的初版规则，即本区「新增」组里那条），缺员照聚、`total` 取**可见步数** —— 宁可按 2 步做完，也不要按 3 步算却永远做不完。"宁可少聚合，不可错聚合"未取消：单成员、`k` 重复、周期或入口不同仍退回单条，只有"缺员"从不可聚合里拿掉。被用户**手动隐藏**的条目不在条目库层，因此不会被复活（那是他的明确取舍，复活了还会被误勾）。另：聚合卡标题行加**菱形进度格**（一格一步，三态与推进器同源：金描边空心 → 朱红描边 = 当前步 → 朱红实心 = 已完成），补上参考稿里被单条卡否掉的那件东西 —— 当初否掉的前提是"布尔勾选没有 `cur/total`"，而聚合卡上它是真的；`ChecklistItem` 的口径随之改口（进度格只属于有真实次数的卡），两处注释同步订正
- **全站换圆润**（用户要求；参考稿换代到 `uiRef/囤囤鼠大作战_控件样式示例.html`）：圆角阶整体放大 3~5 倍（2/4/6/10 → xs8 / sm11 / md14 / lg18 / xl24 / 2xl30）；阴影换成**三级柔和**（`shadow-card` / `shadow-pop` / `shadow-panel`，大扩散 + 低透明 + 顶部 gloss）；新增**半透明填充层** `fill` / `fill-2` / `fill-3`（输入框与内嵌块不再拿 `surface-3` 顶上）；新增 `shadow-ring`（3px 淡环，字段聚焦与选项选中用 —— 键盘焦点仍是 2px 金环，理由写在 `base.css` 的 `:focus-visible` 里）；语义色补齐 `ok` / `warn` / `info` 与各自的 `-soft` 底；字距曲线换成参考稿那一支（`ease-genso`）；图标线宽 1.5 → 1.7
- **明版卡片改为纯白浮起**（本次最大的观感变化）：依据参考稿「明版 · 和纸米白（全阶重算 · 卡片纯白浮起）」——明版卡片从"比面板暗"翻成"比面板亮"，与暗版方向一致；页面底色随之改用最深那一档 `deep`，否则纯白卡压在米白面板上看不出边界。**线色三档按新卡片底全部重算**（烘焙口径见 `theme.css` 铁律 2）
- 明版纸底与暗角再收一档（用户紧接着反馈"亮版的背景是不是颜色太重了"）：页面画布 `deep` 由 `#E7DDC9` 提到 `#EFE8DA`（同时略收黄味），暗角 `--mask` 由 34% 收到 22%。参考稿的 `--bg:#E7DDC9` 是给"面板浮在纸上"的版式配的（它的面板有 24px 圆角 + e2 投影撑着），而本项目的卡是**直接**用 `shadow-card` 摆在纸底上，同样深的纸底就发闷。**这次只动了一个色值**：烘焙基准是卡片底而非纸底，线色与各 `-soft` 底一个都不用重算（上一轮改卡片时才必须全批重算，两者不是一回事）。至此这对关系被从两头各压过一次：卡片降一档、纸底升一档，亮度差从 12 个 L\* 收到约 6 个
- 明版卡片由**纯白下调为和纸白 `#FCF7ED`**（用户反馈"正常状态的卡是不是太白了"）：参考稿的白卡是摆在它自己的 `--panel`（米白）上的，而本项目的卡直接摆在纸黄纸底上，纯白在深底上刺眼。改取参考稿的**面板色**当卡片色，亮度差从 12 个 L\* 收到 8 个，"卡片最亮"的浮起关系不变；`card-done`、线三档、`band`、`gold-soft`、朱红两支与 `ring`、三个语义 `-soft` 底**全部按新卡片底重算**（这批色都是按卡片底烘焙的，换了底不重算就会悄悄偏色）
- 新增 `src/components/common/controls.ts`：把参考稿的控件规格固化成**全站唯一配方**（按钮 7 档 / 输入框 / chip / tag / 选项行 / 浮层 / 卡片 / 徽章）。上一轮的教训是同一颗主按钮在 40 个文件里各写一遍，参考稿一改必漏（`ConfirmDialog` 与 `ProfilePickDialog` 的弹层材质就是这么漏掉的）
- **主按钮换代**：从「金描边 + 淡金底 + 金字」升级为**朱红渐变实心**（参考稿 `.btn.pri`，同一屏最多一颗）；金描边降为次级按钮 `btn.sec`；不可逆动作用 `btn.dan`，列表行内的删除用细描边 `btn.danger`
- `CheckBox` 的**菱形符格保留给任务卡**（示例文件里"点一次菱形推进一步"说的正是任务卡），但**表单类多选改用圆角方块 + 勾号**（参考稿 §10 `.cb`），长按选择弹窗已按此改
- 已换代的组件与位置：分段控件（胶囊外轨 + 朱红实心选中）/ 设置行 / 时间与截止标签 / 币种徽章 / 提示条 / 空态与骨架 / 骨架底轨 / 明暗切换 / 账号切换触发钮 / 长按选择弹窗 / 确认弹窗（毛玻璃遮罩 + 30px 圆角）/ 冷启动引导（删掉文件内两份手写按钮常量）/ 清单卡（**去掉竖条上的菱形挂角**、悬停从"更暗"改为"更亮"、星标改圆头）/ 工具页分段条 / 移动端底栏（去掉签底菱形节点）
- **尚未套用配方**（配方已就绪，见 `controls.ts`）：`ViewBar`（当前被 `SHOW_KIND_FILTER = false` 整体下线、无可见输出，恢复时一并换）、`CollapsibleSection`、`SaveErrorNotice`、`DataVersionSection`、`ItemField`（**有意不动**：图标即标签的行内字段再加填充块，会让每张卡多出 3~4 条阴影带）、以及设置页与工具页各分区内部的输入框与行内按钮
- 移动端顶栏去掉「今日 X 项待做 · 已完成 Y 项」与那条 2px 进度轨（用户要求）：这两样**每个页面各自都有**（今日页的统计条、各页分组头的底轨），顶栏再挂一份等于同一屏说两遍，而顶栏那份只能看不能点；腾出的位置改为副标题「阴阳师任务清单」。顶栏自此只回答"我在哪、我是谁、下一个时间点是什么"，进度一律交给页面自己。`MobileShell` 随之不再引 `useChecklist`（少一次整表派生）
- 全站标题改为 **囤囤鼠**，副标题 **阴阳师任务清单**（用户要求）：两端壳层页头合成同一套品牌锁定；`index.html` 的 `<title>`（`囤囤鼠 · 阴阳师任务清单`）与 meta description、`README` 标题、`AGENTS` 首句、`package.json` 与 `schema/item.schema.json` 的 description 同步。桌面侧栏原有的「纯手动记录 · 不读取游戏数据」一行由副标题取代（同一句免责在 `README`、冷启动引导与「数据备份」说明里都有）。`package.json` 的 `name`（`yys-checklist`）**不改** —— 它是代码层标识与 npm 名，不是标题
- 品牌锁定的字号层级修正（用户反馈"主标题和副标题一样大"）：第一版主标题是 `text-lg`(14.5px) + 副标题 `text-sm`(12px)，只差 2.5px，且**比页面标题**（`PageHead` 的 `text-xl` 16px）还小，而副标题六个字比三个字的主标题更长，读起来就是一样大。改为 **主标题 `text-2xl`(19px) 衬线 + `tracking-title`（5px 宽字距）/ 副标题 `text-2xs`(10px) + `tracking-label`**（差 1.9 倍）；两个字号都取自全站既有声部，没有为品牌另造一级。徽记由 13/14px 提到 16px 与标题配重
- **全项目术语改名：「档案」→「账号」**（用户要求）：界面文案与注释覆盖 7 个页面 / 全部组件 / stores / domain / api / services / hooks / 种子 JSON，加上 `README.md`、`AGENTS.md`、`docs/01`–`04`，共约 60 个文件；`游戏档案` 顺改为 `游戏账号`（保留「游戏」前缀，「设置 · 游戏账号」组同此）。**只改文案，不动代码标识符与持久化键** —— `Profile` / `profileId` / `listProfiles` 等仍是代码层词汇，`yys:profiles` / `yys:state:{profileId}` 等键名是已发布的数据契约（改名会让既有数据读不出来），注释里出现的键名同样保持原样；另一个动作「归档」不含该词、不受影响。测试断言里的错误文案（`至少要保留一个账号`、`没有任何账号`）与实现同步改名；`AGENTS.md` 补「账号 ↔ `Profile`」映射说明；`README` 与「数据备份」里两处"像对待账号信息一样保管账号信息"的同义反复改写
- 工具页分段顺序改为 **结界寄养 / 御魂 / 悬赏**，并默认选中结界寄养（用户要求）：它是三段里唯一时间敏感的（到点要记收/续点），且 `stores/tools.ensure` 对寄养段零请求 —— 从默认分段进入不再触发任何下载，骨架屏只在真的切到御魂 / 悬赏时才出现，与"按需取"的既有口径一致。`navRequest` 的跳转（结界卡徽章 → 结界寄养）不受影响
- 全站换肤为「墨底 / 朱红 / 金箔」三色语言：圆角由 6/8/10/14 收方到 2/4/6/10；字号新增 `2xs`(10px) 与 `2xl`(19px) 两档；组件层 `brand` 系类名整体改为 `gold` / `crimson-*`，实心金底配白字的按钮统一改为「金描边 + 淡金底 + 金字」
- 今日页的**达摩点睛**按用户决策隐藏：`TodayPage` 新增模块级开关 `SHOW_ALL_DONE = false`，页头不再渲染达摩；`allDone` 判定与 `.all-done` 效果链（右眼点亮 / 转朱红 / 统计卡静默）原地保留，改回 `true` 即恢复。零件 `components/ornament.Daruma` 与 `theme.css` 的 `.genso-daruma` 规则一并留着
- 设置页与工具页**清掉一批说明文案**（用户要求）：删除纯复述界面与实现细节的段落 —— 寮时间的功能说明与账号口径、账号同步的开场说明、账号列表页脚、卡片字段说明块、条目管理页脚、悬赏页的操作提示、御魂页的导读段、寄养页的表单说明与页脚；长段压缩为一行（一键日常的覆盖集合说明、寄养的推点规则、御魂的副本概览）；「奖励类型（可多选，只标类型不填数值）」这类改写为紧凑标签。**刻意保留**：① 空态引导（单账号 / 无匹配 / 无进行中 / 无计划）—— 去掉后用户不知道下一步；② 「只影响列表」「不影响统计口径」这类限定语；③ 御魂页脚的数据口径与存疑声明（"各源口径不一，以游戏内为准"）—— 属项目红线「不假装确定」。顺带修正寄养图例里过期的颜色词（原写"橙=该收了"，换肤后该态已是金色）
- 设置页页头整块去掉（用户要求）：「设置」标题、账号切换器、御币横饰一并移除 —— 三处都是页内重复（页面名左栏/底栏已高亮；账号切换器两端壳层各常驻一个、且「账号」组的「游戏账号」也能切号；御币是纯装饰）。顶部内缩由 `py-5` 收到 `pt-1`（分组头自带 `pt-4`，合计仍约 20px），首屏直接进「观感」组
- 设置页整体重构（用户要求：该合并的合并，对齐参考稿的设置页）：**9 个平级折叠卡收成 4 组** —— 观感（界面主题 · 卡片显示字段）/ 委托（一键日常覆盖 · 条目管理）/ 账号（游戏账号 · 同步到其他账号 · 寮时间）/ 数据（数据版本 · 数据备份），组名复用清单页的分组头（`SectionTitle` 新增 `flush`，供父容器已自带内缩的页面使用，否则会缩两次）。组内形态按「**能一行说完的平铺、需要展开看的收起**」：新增 `SettingRow`（参考稿 `.srow` 材质，与任务卡同一套 `surface` + `shadow-card` + `rounded-md`）与 `Segmented`（分段控件，`value: null` 表示不属于任何一档、三档都不高亮）两个原子件；界面主题 / 数据版本 / 卡片档位改为平铺设置行，5 个长列表仍默认收起。`CollapsibleSection` 视觉对齐 `SettingRow` 且不再自带 `mt-4`（组内间距统一由 `space-y-1.5` 给）。「卡片显示字段」由「3 个预设行 + 6 个逐项行 + 预览」改为「**档位分段控件一行 + 逐项与预览折叠**」，行内说明随选中档位联动（省掉罗列三档描述的一半高度）
- 移动端顶栏重排为三排：①「徽记 + 标题 ⋯ 明暗切换（纯图标）+ 账号切换」；②「今日 X 项待做 · 已完成 Y 项 ⋯ 结界卡倒计时（最右）」；③ 2px 进度轨。`ThemeToggle` 新增 `compact` 纯图标形态（图标仍是**将要切到**的那一版，`title` / `aria-label` 写全作为唯一语义出口），`components/icons` 新增日月两枚符号；结界卡徽章由标题行移到进度行最右，标题行不再同时挤三个控件
- 移动端的筛选入口不再独占一整行，改由 `PageHead` 新增的 `action` 插槽收进页头右侧（PC 仍保留整行 chip），入口带**生效计数**、面板改 popover 并给出「清空筛选」；随后按用户决策整体下线，见下条
- 奖励类型筛选整体下线（用户决策「暂时隐藏」）：`stores/view` 新增总开关 `SHOW_KIND_FILTER = false`，`components/common/ViewBar` 在关闭时整条不渲染（四个清单页的调用保留，恢复只需把这一个常量改回 `true`）。**并同时关掉生效链路** —— `hooks/useChecklist` 与 `pages/LimitedPage` 在开关关闭时一律传 `showKinds: []`：只隐藏控件却继续过滤，会让「清单莫名少几条」变成查不到原因、也清不掉的死角。`ViewPrefs.showKinds` 字段、`setShowKinds` action 与 `domain/sort.isVisible` 的规则原样保留
- 限时页页头去掉与下方警示条**逐字重复**的"N 个活动在跑 · 临期几天"
- 一级页面「我的」改名为「设置」：导航标签（`NAV_ITEMS`）、页内标题、以及全部指向该页的说明文案（今日 / 限时页的空态提示、`HubCard` 与 `ProfilePickDialog` 的标题、冷启动引导三步）与 `README` / `docs/04` 同步。**内部 `NavKey` 仍为 `me`**，路由、锚点跳转（`requestNav`）与分区定位逻辑不变
- `ChecklistItem` 重构为「委托札」：左侧 3px 状态竖条 + 上下菱形挂角、任务类型符（按 `cycle` 枚举映射，不由 `path` 文本反推）、衬线任务名 + 朱红划除线、已完成时蛇目纹紧贴标题右侧、卡底 2px 底轨（完成时朱红满格）、已完成卡改用 `card-done`；新增 `highlight` 属性表达"本页唯一高亮位"（限时页给 ≤3 天临期条目）
- `HubCard`（一键日常入口卡）接替「唯一高亮位」：淡金底 + 金描边 + 悬停金晕，图标由内联闪电换成和风金槌，「去设置」改金描边按钮
- `CheckBox` 由「圆形绿底白勾」改为「菱形符格」：未勾金描边空心、已勾朱红实心，完成态靠卡片沉降与划除线表达而不再亮一个绿点
- `SectionTitle` 改为朱印分组头（图标 + 衬线组名 + 虚线 + 计数 pill），并支持 `progress` 渲染 2px 真实完成比例底轨；`EmptyState` 空态图标换成鸟居，`Skeleton` 对齐新卡面
- 双端壳层：PC 侧栏与移动端顶栏各自铺底纹，壳层根节点去掉不透明底色让全局底纹透出；移动端**底部 Tab 位置不变**，改为「符纸签」造型（选中签顶部 22×2 朱红短线 + 底部菱形节点 + 图标），PC 侧栏导航项改为竖排符纸签（选中 = 卡底 + 金描边 + 左缘朱红短线）
- 今日页新增符纸签式「常驻 / 活动」双 Tab，页尾加注连绳与桔梗纹带收束；本周 / 本月 / 限时三页接入 `PageHead`（衬线标题 + 唯一的等宽倒计时）
- 统计页日历热力由品牌紫改为朱红分档、深档字色改 `surface-2`（明暗两版都压得住），三币种卡改朱印卡面并给数值加等宽
- 徽章体系统一为「细描边 + 极淡同色底」：`Tags`（时间窗 / 截止）、`GainBadges`（固定收益 / 浮动类型 / 一键 / 付费前置）、`ItemField`（入口 / 条件 / 备注）的图标全部换成和风符号
- 弹层与通用壳件（`ConfirmDialog` / `ProfilePickDialog` / `OnboardingDialog` / `ProfileSwitcher` / `SaveErrorNotice` / `CollapsibleSection`）：遮罩改 `scrim`、外壳改 `surface-3 + shadow-panel`、危险动作由实心朱红块改细描边（**其中 `ProfilePickDialog` 与 `OnboardingDialog` 当时漏改**，见下条）
- 「同时勾选到其他账号」弹窗（`ProfilePickDialog`，长按清单条目触发）与冷启动引导（`OnboardingDialog`）补齐弹层材质：这两个当时漏改，外壳仍是 `bg-surface`（= `--c-card`，明版 `#E3D7C1`）+ `shadow-lg` —— 比它们底下的页面底 `--c-panel`（`#F5EFE3`）还**暗**，浮起方向反了，从页头到页脚糊成一整块重色。现改回 `bg-surface-3 + shadow-panel`（与 `ConfirmDialog` 同口径）。同批修：标题补衬线（`font-serif tracking-card`）；「取消」与「跳过引导」的 `hover:border-line` 与自身边框同色、是个空转的 hover，改 `hover:bg-surface`；勾选指示器由方框改为**菱形符格**（与清单卡勾选控件同形状语言：金描边空心 → 朱红实心），且写成行内 `<i>` 而非嵌套 `button`（行本身已是 button，再套一层会产出非法 HTML）；「当前账号」一行改成一条 `bg-surface` 纸片，让"只读 / 可点"在**材质上**就分开，不必靠一行小字解释
- `index.html` 增加 `data-theme="light"` 并按 `prefers-color-scheme` 给出两条 `theme-color`；`main.tsx` 引入 `theme.css` 并在渲染前 hydrate 主题

- 结界寄养的任务行**移动端改为三层**（用户要求）：第一层「上卡信息 · 统计」、第二层「时间线」、第三层「操作条 · 删除」。窄屏下操作条挤在第一层会把时间线顶得更窄，而"记完成"恰恰是**看完时间线之后**才做的动作 —— 放在线下方，视线顺序与操作顺序一致。桌面端保持两层（宽度够，三层会显得松散），用 `md:flex` / `md:hidden` 二选一显示；操作区只写一份 JSX（`actions` / `delBtn`）两处复用，不抄第二份
- 结界寄养的任务行**改为两排**（用户要求「让时间在单独一排」）：第一排「上卡信息 · 统计 ·（右推）操作条 · 删除」，第二排「时间线」独占整行。时间线本就是一条横向序列，与操作条同排时会被挤成两三行反而更难读；拆开后原先那个 `min-w-36` 的最小宽度约束（防 chips 被压到 0 宽后溢出到操作条上）连同它要防的问题一起消失 —— 布局层面解决比打补丁稳
- 结界寄养的**结束时间从左侧信息区移到右侧时间线末尾**（用户反馈：分开放不方便）：原先它竖排在「上卡 / 持续」下方，与点分离两处，看"最后排到几点、离卡到期还差多久"要来回扫；现在作为时间线的最后一个 chip，一眼能看出它比最后一个点晚多少（22h 的卡：最后一点 02:00、结束 06:00）。新增 `endPointOf`（`index = -1` 哨兵值，复用 `shapePoint` 以与收/续点共用同一套日标签口径，且天然不会被选中）；chip 用虚线边框 + 浅字色，读作"结束·明天"，只读不可点。左侧信息区随之少一行，宽度从 `w-28` 收回 `w-24`
- 结界寄养新增**「延迟」字段**（分钟，0 – 60，默认 0）：递推变成「第 k 点 = 上卡 + `k × (6h + delay)`」，延迟**逐点累积**（6:00 上卡、延迟 5 → 12:05 → 18:10）。**算点数时带上延迟**：`pointCountOf` 从 `floor(hours / 6)` 改为 `floor(hours × 60 / (360 + delay))`，所以 24h 的卡配 5 分钟延迟只有 3 个点而非 4 个（第 4 点会落在 24h20min，超出卡的寿命）。结束时间**不受延迟影响**（由持续时间决定）。落盘 `NurtureRecord.delay`，`sanitizePlans` / 备份归一化把**缺失的 `delay` 当 0**（可选细节，不因此丢整条）、**离谱的夹紧**（同 0 一样是细节，没必要废掉记录）；递推间隔统一走 `nurtureStepMs(delay)`，别处不再写 `6 * 3600000`
- 结界寄养表单**不再让用户选推点数，改为填「结界卡持续时间」**（小时），点数由 `pointCountOf(hours)` 派生（22h → `floor(22/6)` = 3 个收/续点），并新增**只读的结束时间** = 上卡 + 持续时间。理由："排几个点"是寄养机制的内部换算，用户手里的事实是"这张卡能撑多久"，不该让人心算 22/6。数据层 `NurtureRecord.n` → `hours`（点数**不落盘**，由 `hours` 派生 —— 存两个字段迟早漂移：改了时长忘了改次数，就会出现"说 3 个点、显示 5 个"）；新增 `endTsOf` / `endLabelOf` / `pointCountOf` 与 `MAX_NURTURE_HOURS`（= 点数上限 × 间隔，避免"排得出点、但记不下"）。结束时间不能用 `点数 × 6` 反推：最后一个收/续点之后卡还会继续生效（22h 的卡，最后一点在 18h、结束在 22h）。`sanitizePlans` / 备份归一化同步按 `hours` 校验
- 结界寄养的**两个时间框进入页面即默认填入当前时间**（用户要求）：新建的「上卡时间」与每行的「实际完成时间」此前都是空白 + `placeholder` 提示"留空 = 现在"。默认值**跟着时钟走**而不是取进门那一刻的快照 —— 否则停留十分钟后按提交，记的会是进门时的时间；用户一旦手动输入（含清空）就以输入为准。「用现在」按钮改为复位成"跟随"（`setTime(null)`），`doneDraft` 的重置改为**删键**而非置空串（空串会让输入框显示空白，而缺键才回到"显示现在"；`??` 对空串不生效，这是 fallback 用「键是否存在」而非「值是否为空」的原因）
- 截止倒计时**不足一天时改按小时显示**：`daysLeft` 的 `Math.ceil` 会把「还有 2 小时」算成「剩 1 天」，限时活动的最后一段这个粒度太粗 —— 「剩 1 天」让人以为还有一整天，实际马上要结束。现在 `deadlineBadge` 在剩余不足 24 小时时输出「剩 N 小时 · 10/6 23:59 止」，并新增 `hoursLeft` 纯函数（与 `daysLeft` 成对，同一次时间差的两种刻度）。`daysLeft` 本身**不改**：排序与 `LimitedPage` 的 urgent 计数依赖它按天粗粒度的语义。`DeadlineBadge` 新增 `hours` 字段与 `days` 并存，颜色分级（≤3 天红）继续走 `days`
- 「条目管理」为**属于一键日常覆盖集合**的条目加上「日常覆盖」徽章（中性灰，与「自建」的品牌紫区分开 —— 两者含义不同，同色等于都没说清），搜索栏旁补一句说明。数据取 `useAutoDaily().coveredSet` 而非直读 `view.autoSet`：前者是 `effectiveAutoSet` 归一后的集合（只含常驻每日、已剔除失效 id），与入口级联勾选的口径完全一致，不会出现「标了却不会被入口勾上」。隐藏某条后徽章随之消失，也是同一原因
- 一键日常入口卡（`components/common/HubCard.tsx`）**跟随「视图偏好」**：此前它是独立组件、不受卡片字段开关影响，用户关掉入口路径 / 备注后今日页第一张卡仍是最高的，"卡片可以只留名字"在最该生效的位置上失效。现在 `path` 与 `note` 两块同样受控（`tags` / `gain` / `kinds` 在本卡没有对应渲染块），且 **`path` 关闭时仍保留「已配置覆盖 N 项」** —— 那是入口卡的核心信息，不属于"入口路径"这个字段
- 壳层常驻「下一个结界卡收/续点」徽章（`components/common/NurtureBadge.tsx`）：桌面侧栏底部与移动端头部、紧挨账号切换器；每 60s 自己走时，到点显示「该收卡了」，点击直接跳到工具页的「结界寄养」分段。取点规则落在 domain（`nextDue` / `dueText`，带单测）；`stores/ui` 的一次性跳转标记由 `sectionRequest` 泛化为 `navRequest`（页面 + 分区），今日页「去设置」同步改用；移动端为紧凑形态（仅「图标 + 剩多久」）与账号切换器同一行，桌面端保留完整时刻
- `README.md` 追加「开发者文档」一节，指向 `AGENTS.md` 与 `docs/`（只加索引，不复述正文）
- `.gitignore` 忽略 IDE 目录 `.codebuddy/`
- 条目管理的周期分组由「常驻全展开」改为**按需展开**：点周期名才展开该组（同一时刻最多一组），有搜索词时强制展开全部命中分组 —— 90+ 条条目不再一次铺满好几屏

- 「已隐藏的预设条目」区同样按周期分组展示、按需展开（展开状态与上方列表独立），恢复按钮补上 `aria-label`

- PC 端左下角的账号切换器改用 `compact`（与移动端头部一致，只显示账号名）—— 侧栏只有 224px，带区服时区服会被截断，反而看不全

- PC 侧栏左下角的「当前账号」标签与账号切换器改为同一行（原先竖排两行，224px 宽的卡片里空白过多）

- 限时页的截止徽章原先独占卡片右侧一列，把内容区压窄、导致下方备注提前换行（而备注最长的条目恰好都带 deadline）：改为渲染在标题行内，内边距与同行徽章统一为 `py-0.5`

- 周期刷新口径更正：每日与周常都是 **0 点**刷新（此前按 05:00 推算，`meta.resetHour` 5 → 0，`meta.resetNote` 同步改写）；版本 / 赛季仍按开服锚点，锚点即版本上线当日维护完成的时刻（通常 9:00）。本周页的重置提示移到限时页的「版本 / 赛季」分区，README 中「为什么不 0 点重置」的 FAQ 按新口径改写

- 补充开发阶段约定（`AGENTS.md` 红线第 9 条 + `docs/03` 第 8 节）：项目尚未发布，改动**不必考虑历史数据兼容**（旧分片、旧备份、已失效 id 都不必写迁移路径），但仍保留「运行期就会出现」的廉价防御（不存在的 id 过滤、已下线键清理、越权校验）

- 统一措辞（纯注释与用例名）：`domain/autoDaily` 及相关用例里「兼容旧配置」的说法改为「已失效 id / 运行期脏数据」—— 这些过滤防的是**运行期**情况（条目被删、活动条目下线），不是跨版本兼容，按新约定容易被误删

- 开发依赖升级：`vite` 5 → 8、`vitest` 2 → 5、`@vitejs/plugin-react` 4 → 5（`npm audit` 5 个漏洞清零；构建、单测、数据校验已验证通过）
- AGENTS.md 增加「提交身份要显式指定」约定（红线第 10 条）：本机未配置 git user，提交时用环境变量 + `--author` 指定 `Jachin-Luo <Jachin-Luo@users.noreply.github.com>`，不改 git 配置；同时修正了本轮两次提交的身份

- 今日页一键日常入口卡右侧按钮由「去完成」改为「去设置」：原按钮与「点整张卡」效果完全重复，现在点击跳到「我的 · 一键日常覆盖」并自动展开该分区（`stores/ui` 新增一次性跳转标记 `sectionRequest`，`CollapsibleSection` 支持 `id` 锚点）

- 结界寄养的完成记录改为**逐点**：上卡时刻也作为一个点显示在任务里（index 0，只读），每个收/续点可点选并单独「记完成 / 改时间 / 取消完成」；第 k 点的预计时刻 = 前一点的「实际完成时间（没有就用它的预计时刻）」+ 6h，因此记完成只让它**之后**的点顺延、之前的点不动 —— 取代此前「一个按钮把整条任务重推」的做法（那种做法看起来像把任务初始化了）。数据层以 `NurtureRecord.dones` 取代 `lastDoneAt`，`pointStats` 改为「已完成 / 待收」，取点统一走 `recordPoints`（展示、徽章、`nextDue` 共用），`hmToDate` 供输入解析，带单测

### 审查修复（2026-09-16 自查）

- **乐观更新 + 失败回滚的竞态**（`stores/nurture` / `view` / `guildTime`）：只写了"失败就回滚到 `prev`"，没有考虑**旧写的失败赶在新写之后返回**。连点两下时，第一次的 catch 会把第二次的结果一起抹掉 —— 内存回滚成旧值而磁盘已是新值，用户看到"刚改的东西凭空消失"。修法是加写入序号 `writeSeq`，**只有最新一次写入的失败才允许回滚**；`reset*Memory`（切号）也递增它，让上一个账号在途的失败不再回滚到新账号上
- **一键日常入口卡的跨账号勾选带级联**：初期实现里长按只写入口这**一条**，会造成目标账号"入口已完成、被覆盖项没勾"的不一致状态（统计口径上最难被发现的那类坏数据）。`stores/check.toggleInProfiles` 改为收**一组** id（整组共用一个时间戳、日志按整组读写一次）；`ChecklistItem` 新增 `cascadeIds` 由调用点传入覆盖项；`HubCard`（未完成态的入口卡，独立组件）也接上同一套长按手势与进度反馈 —— 现在**四个清单页与入口卡的跨账号勾选行为完全统一**
- **寄养记录在读取入口净化**：分片字节与备份文件都是**外部数据**，一条 `base` 非法的记录会让 `recordPoints` 递推算出 `NaN`（界面显示 `NaN:NaN`、所有点全乱）。净化函数从 `domain/backup` 的私有实现提升为 `domain/nurture.sanitizePlans`，**分片读取与备份校验共用同一个入口**（规则只写一遍，两处不会漂移）
- **`domain/sync.applyParts` 对 `plans` 的浅拷贝**：`dones` 是嵌套对象，只做一层拷贝会让补丁与原记录共享它 —— 之后给源记完成会"隔着补丁改到目标账号的数据"。改为连 `dones` 一起复制
- **`effectiveCardDisplay` 返回常量引用**：无参时曾直接返回模块级 `DEFAULT_CARD_DISPLAY`，调用方把它存进 state 后一旦就地修改就会改掉全局默认值。改为返回副本

### 修复

- **清单卡片「奖励」类型徽章的字号**（用户反馈"清单中，奖励的字体大小太大了"）：`GainBadges.tsx` 里 `KindBadges` 的那枚**虚线类型徽章**（「达摩 / 皮肤券 / 御魂」这类）原先**没有字号类**，于是继承**根字号 16px** —— 比同一行的标签（12px）、比上一行「固定收益」的徽章（12px）、甚至比**任务名**（14.5px）都大，读起来像"奖励在喊"。补 `text-sm` 后，奖励块的四个元素同属 12px 一族：两个标签（「固定收益」「另有 / 含 + 数量不固定」）+ 两类徽章。聚合卡与单条卡共用这两个组件，因此一处改、两处生效。⚠️ 这类错的特点是**全程不报错**：漏写字号的元素不触发任何 lint，只是悄悄变成 16px（Tailwind preflight 不给根元素设字号，`body` 的 `@apply` 也只有颜色与字体族）。字号链已逐层核过（`App` → 壳层 → `NavContent` → 页面 → `CHECKLIST_GRID` → 卡片 → 徽章容器，无一设定字号），判据写进了组件注释
- 清单**长按**的两处问题：① 长按进度条被挪到了卡片**顶部**（上一版为给 2px 底轨让位），按压反馈跑离手指落点 —— 改回贴**底边**，与底轨共用同一条边、且渲染在底轨之后压在上面（两者都是 2px，不会互相误读）；一键日常入口卡同步处理。② 长按时会**选中卡片文字、并连带选中相邻卡片的文字**（松手后满屏高亮）—— 新增 `styles/base.css` 的 `.no-press-select`（`user-select: none` + `-webkit-touch-callout: none`）挂在两张卡上。**刻意没有**在 `useLongPress` 里对 `pointerdown` 调 `preventDefault()`：那个事件会冒泡到整卡，连带把需要聚焦的菱形勾选框一起挡掉（点它不再获得焦点），为一个选择问题牺牲键盘可达性不划算

- 四个清单页（今日 / 本周 / 本月 / 限时）底部长出**横向滚动条**：装饰零件 `KikyoBand` / `Gohei` 把 `w-full` 与调用方传进来的 `mx-3.5` 挂在**同一个元素**上 —— `w-full` 解析的是父容器宽度，再叠加左右各 14px 外边距，总宽就溢出 2×margin。改为「外边距挂外层 div、`w-full` 留内层 svg」（`Alert` 早就用 `w-[calc(100%-1.75rem)]` 绕开了同一个问题）。同时全库排查了一遍 `w-full` + 横向外边距的组合，无其他实例

- 两端的**账号切换弹窗被遮挡**：换肤时为了让底纹贴合容器给 `DesktopShell` 的 `aside` 与 `MobileShell` 的 `header` 加了 `overflow-hidden`，把 `ProfileSwitcher` 的 absolute 下拉整片裁掉（PC 向上弹、移动端向下弹，两处都中）。底纹 `Texture` 用 `absolute inset-0`，本就不需要父级裁剪，两处 `overflow-hidden` 移除；`styles/base.css` 里原有的警告注释扩写成"不要给装着 `ProfileSwitcher` 的容器加 overflow"，两个容器的 JSX 处各留一条就地提醒
- `tailwind.config.ts` 的 `slide-up` / `fade-in` 关键帧把 `opacity` 写进了 `@keyframes`：动画一旦被冻结（headless 截图 / `prefers-reduced-motion` / 扩展拦截）该元素会永久停在起始帧而整块不可见。改为只做位移的 `reveal`，`ViewBar` 的筛选面板随之换类名

- 结界寄养「改时间」后**那个点自己的时刻不变**，只有它之后的点跟着挪（用户反馈："修改的当前时间不会变，只有后续的时间会变"）。原因是 `recordPoints` 一律用预计时刻 `ts` 渲染，而 `dones[k]` 只参与下一点的递推 —— 用户把「14:00 收」改成「14:05 收」，那个点仍写着 14:00，看起来像改动没生效。现在已完成的点显示**实际完成时刻**（`hm` / `dayLabel` / `past` 统一取 `doneAt ?? ts`），`ts` 保留为递推用的预计值。同步修正操作条提示与说明文案（原文案只说"之后的点会顺延"），单测 3 处断言按新口径更新，并补一条「`hm` 取实际、`ts` 仍是预计」的用例
- 统计 / 工具 / 我的三个页面在桌面端内容整体贴左：三页各设了宽度上限（统计 768 / 工具 896 / 我的 672），但容器没有 `mx-auto`，在 1024 的桌面内容容器里左对齐、右侧空出大片留白。三页容器补 `mx-auto`，并同步修正 `DesktopShell` 与 `StatsPage` 中「居中职责只在 Shell 一层」的注释口径

- 「我的」页展开折叠分区时页面横向错位一下：`main` 是唯一内层滚动容器，展开内容使高度跨过"是否需要滚动"的阈值时，滚动条（全局宽度 8px）出现 / 消失会让可用宽度变化，内部的 `mx-auto` 容器随之重新居中。给滚动容器加 `scrollbar-gutter: stable`（`src/styles/base.css`），槽位常驻、宽度恒定

- 一键日常的覆盖候选被放宽到「全部常驻每日」（35 条），斗技、逢魔之时、地域鬼王、寮活动、御魂副本等官方不会代做的任务也能勾进覆盖集合 —— 勾选入口时级联会把它们一并标成已完成，等于伪造进度。判定收窄为「常驻每日 + 数据标了 `autoDaily`」（现 14 条），与 README 的覆盖项口径一致；旧配置里混入的此类 id 在归一化时自动失效

- `tools/verify.js` 的单测命令由 `--reporter=basic` 改为 `--reporter=default`（vitest 5 移除了 basic reporter，原参数让验收在启动阶段报错、一个用例都跑不起来）

### 移除

- **`ViewPrefs.hideDone`（「隐藏已完成」）与它的豁免口 `keepDone`**（用户要求，2026-09-24）：它是全场唯一一条**界面上碰不到、代码里却仍生效**的筛选 —— 用户既改不了它、也没有任何地方能看出它开着，而它会在按次数聚合之前把已勾条目从数组里抽走（正是"分组静默散开"的成因之一）。删的理由不是"没人用"，而是**边界要清楚**：「已完成」在本项目一贯的表达是**沉下去但仍在**（`card-done` + 划朱线 + 独立「已完成」分区），"勾了就消失"从来不是这套语言的一部分。落地范围：契约字段（`ViewPrefs` / `ViewDefaults`）、`domain/merge.effectiveView` 的回落、`domain/sort.isVisible` 的规则与它的 `checkedAt` 参数（此前只服务于它）、`stores/view` 的 `toggleHideDone` 与 `FALLBACK`、`domain/sync` 的 viewPrefs 同步项、两个页面（`useChecklist` / `LimitedPage`）的筛选上下文、种子（`users.db.json` 与 `meta.db.json` 的 `viewDefaults`）、6 个测试文件，以及 `AGENTS.md` 与 `docs/01`–`04` 的口径表（`docs/04` 的"保留但不可达的开关"一条据此改写）
- 今日页的统计三卡（常驻待做 / 活动待做 / 已完成）与四个清单页（今日 / 本周 / 本月 / 限时）`PageHead` 下方的御币横饰 —— 评审认为这两处横排装饰在首屏抢视线。零件本身保留（`components/ornament/` 的 `StatStrip` / `Gohei`），随时可复用

- `lucide-react` 依赖：26 个图标名 / 约 50 处引用全部换成本仓库自建的和风图标库（`package.json` 与 lockfile 同步）
- `tailwind.config.ts` 的 `brand` / `brand-soft` / `brand-deep` 令牌及组件层全部引用（品牌紫退场，主色改为金箔、强调色改为朱红）

- 统计页的「可量化条目」列表与「漏失明细」分区：前者与清单页重复（同一条目在今日页也能勾），后者只复述漏了哪些条目。`domain/stats.ts` 的 `periodItems` / `summarizeGain` 保留（有单测保护，随时可恢复；`missGroups` 已于 2026-09-15 删除，见下条）
- 痛感从「多用途」收敛为**只作默认排序键**（2026-09-15），四处一并移除：① `minWeight` 筛选门槛（`ViewBar` 的选项 chip、`domain/sort.isVisible` 的门槛判断、冷启动引导第三步的「首屏显示范围」选择）；② 今日页「本周高痛感还剩 N 项」警示条（此前被 `SHOW_WEEKLY_ALERT = false` 关着，连同 `useChecklist.weeklyHighWeightLeft` / `HIGH_WEIGHT` 一起删除）；③ `domain/stats.missGroups` 漏失分级（早已无页面消费）；④ `domain/weight.WEIGHT_LEGEND` 图例数据（本就无消费方）。`ViewPrefs.minWeight` / `viewDefaults.minWeight` 字段与 `stores/view.setMinWeight` 按 `sortBy` 的先例保留在数据层（不动契约形状），注释已标注废弃

### 数据

- 御魂提示文案去「口碑 / 建议」类表述（用户要求）：删掉 2 条 —— 魂王（`orochi_shinpan`）的副本说明（"掉率口碑差，日常不建议刷；官方「御魂自选」时优先刷它。新御魂也可从秘卷屋/秘魂屋换到"）与魂十周四的 7 日条提示（"针女老牌输出，心眼适合残血收割"）。前者按可选字段**整键移除**（`Dungeon.tip` 本就可缺省），后者**整条移除**（`DayTip.tip` 是必填字段）；删除后周四按既有回落逻辑显示副本级说明（`domain/yuhun.dungeonDay` 的 `dayTips → dungeon.tip` 回落），不留空洞。副本、掉落与轮换表数据一行未改；`dataVersion.db.json` 的 yuhun 行随之升为 `2026.09.24-提示文案去主观评价`
- 修正 `ltd_shenjuan_yongheng`（神眷符咒·永恒兑换）缺失的 `start` 字段（补为 `2026-09-09`，与同期且 deadline 相同的拾光金眷一致）—— 它是 24 条 `limited` 里唯一缺开始日的条目
- 条目库划分口径收紧：`items.db.json` 只留**真正的常驻**（每日 36 / 每周 29 / 每月 4 = 69 条），把 5 条版本与 1 条赛季条目（版本活动爬塔积分、活动商店兑换、斗技赛季奖励、皮肤返场 / 限定兑换、寻契心缘商店、曜之阁·星光契兑换）移入 `limited.db.json`（16 → 22 条）—— 这些玩法并非每个版本都有，不属常驻。`cycle` 字段未改，因此页面归属（限时页的「版本 / 赛季」分区）、重置锚点（`meta.periods`）与统计口径均不受影响；`api/mock/db.ts`、`tools/build.js` 的双文件说明与 README / `docs/02` 的规模数字同步更新。条目总数仍为 91 条
- 活动期条目库随 2026-09-16 维护更新扩充：`limited.db.json` 新增 12 条（9/16 新增活动 6 条 —— 炼石成金·二阶段首领战、天域回响·皮肤祈愿、神秘商人的秘密、祈胜秘诀·斗技UP、今忆召唤·概率UP、运势加成+丰年探索；十周年开服漏录 6 条 —— 大地奉引·凤凰涅槃召唤、祈愿福礼、百鸟逐焰、十周年时光机、SSR/SP重临神龛、灵狐新梦·晴明皮肤免费领），并修正 2 条既有备注（`ltd_miwen_guanzhong` 补「棺中奇梦·番外」限时挑战与聊天挂件、`ltd_shiguang_yongheng` 补二阶段首领战窗口）；**无到期删除**，截至 2026-09-17 无 `until` 已过条目（最近到期为 09-23 的锦鲤福钓场 / 星聚回归）。条目总数 91 → 103 条，快照版本 `2026.09.13-十周年勾玉查漏` → `2026.09.17-十周年二阶段`（`meta.dataVersion` / `meta.updated` / `meta.gainKindNote` 计数、`dataVersion.db.json` 的 meta 与 limited 两行、`AGENTS.md` 与 `docs/` 五处抬头、`README.md` / `docs/02` / `docs/04` 的规模数字同步更新）

### 修复

- **结界寄养任务在移动端的布局重叠**：点 chips 的容器只有 `flex-1 min-w-0`，`flex-basis: 0` 且可压缩到 0 宽 ⇒ 外层 `flex-wrap` 永远等不到"空间不足"，它选择压扁这一块而不是换行；被压到 0 后里面固定内容宽的 chip 溢出自身盒子、画到右侧「记完成」操作条上（桌面够宽所以看不出来）。改为给容器一个最小宽度（`min-w-36`），空间不足时正常换行；操作条去掉 `flex-none` 并加 `min-w-0`，窄屏下改在自身内部换行
- 顺带修正寄养段两处**已过期的说明**：文件头注释仍写"数据落在设备级键"，页脚仍写"只存在本机（设备级，不随游戏账号走）" —— 寄养记录自 2026-09-16 起已改为**账号级**分片并随备份走

### 清单卡片

- **设置页新增「视图偏好」：卡片显示哪些内容由用户决定**（2026-09-16 用户需求）。此前每张卡固定渲染「名称 + 四类徽章 + 入口 + 条件 + 备注」，信息完整但在手机上很高，只想打个卡的用户要划很久。现在可关掉六项中的任意一项（时间 / 截止徽章、固定收益、奖励类型、入口路径、参与条件、备注），并提供三档预设（**极简** = 只有名称 / **简要** = 名称 + 徽章 / **完整** = 全部）。数据落在 `view.card`（账号级、随备份走）；**默认值与"全部显示"完全一致**，因此该功能不改变任何既有观感。卡片里的**名称、勾选框、☆ 置顶不可关** —— 它们不是内容而是身份与交互，关掉只会制造"卡片空白 / 勾不了"的坏状态
- 新增 `domain/cardDisplay`（预设 + 归一化 + `matchPreset` + `hiddenFieldCount`）与 `pages/settings/CardDisplaySection`。预设与逐项开关**是同一份数据**：改任一项后不再匹配任何预设、UI 相应不高亮 —— 不会出现"预设说 A、开关说 B"。设置页带**真实卡片预览**（用清单里信息最全的那条渲染一张真卡，`pointer-events-none` 点不动，避免在设置页里误勾条目）。`domain/sync` 的「清单显示偏好」同步项一并带上 `card`

### 账号与配置

- **寮时间与结界寄养任务由设备级升为账号级**（2026-09-16 用户决策）：两者都不是"这台手机的属性"—— 寮时间取决于所在寮（代管他人的号、或两个号分处两寮时，一份配置会互相污染），寄养节奏取决于具体号的结界卡（太鼓 / 斗鱼 / 美食卡，6h / 12h…）。改账号级后它们同时**进入备份**：`清理站点数据 → 导入` 不再永久丢失这两项。新增分片 `yys:guild:{profileId}` / `yys:plans:{profileId}`，`BootstrapPayload` 与 `UserDataBundle.data[]` 各带两个字段，`DEVICE_KEY` 收敛到只剩 `yys:onboarded`（它不是配置，而是"这台设备看过引导没有"，不进备份）。旧的两个设备级键**按用户决策不做迁移**（无读取点，用户重配一次即可）
- **契约新增 5 个方法**：`getGuildTime` / `saveGuildTime` / `getPlans` / `savePlans` / `getCheckLog`。最后一个供跨账号勾选读**目标账号**的日志（日志的常规读路径 `getBootstrap` 只覆盖当前账号），对称性上它本来也该有
- **设置页新增「同步到其他账号」**：目标账号与同步内容（寮时间 / 寄养任务 / 一键日常覆盖 / 自建条目与排序 / 隐藏条目 / 清单显示偏好）都由用户勾选，执行前二次确认，部分失败如实汇总。领域侧 `domain/sync.applyParts` 做**字段级接管**（只勾「一键日常覆盖」时，目标账号的筛选与置顶不动），服务侧 `services/profileSync` 循环调用既有 `saveXxx`（**零新契约**）。**勾选状态与勾选日志刻意不在可同步清单里** —— 那是每个号的进度，整表覆盖会直接抹掉另一个号的记录
- **清单长按 = 跨账号勾选**：长按条目（PC 上右键等效）弹出 `ProfilePickDialog` 选择目标账号，确认后这条同时写进选中的其他号。按住途中卡片下压 + 品牌描边 + 500ms 底部进度条（"正在被长按"的可见反馈），位移超过约 8px 视为滚动、立即取消；触发后吞掉尾随 `click`（否则会顺手把当前账号也勾上）。默认不选任何账号 → 零误写；选择只记内存。手势抽成 `hooks/useLongPress`（样式留在调用方），写盘的其他账号**一并维护 `checklog`**，因此它们的统计日历不会缺格子
- 备份摘要与文案同步：导入前计数从 5 项增至 8 项（+ 日志天数 / 寮时间 / 寄养任务），导出说明改为"全部账号的全部配置"，并注明唯一例外是设备级引导标记。`stores/nurture` 的落盘改为契约异步（失败回滚），`stores/device` 瘦身到只剩引导

> 本轮基线（记录起点）：`npm test` 19 个测试文件 / 251 个用例全绿；`npm run dev` 可访问 `http://localhost:5173/`；条目库数据版本 `2026.09.13-十周年勾玉查漏`。

## [1.4.0] - 2026-09-14

### 新增

- 首次公开发布：六个一级页面（今日 / 本周 / 限时 / 统计 / 工具 / 我的）
- 本地 Mock API 适配器 + 按账号分片的 `localStorage` 持久化（勾选 / 视图 / 覆盖层各自分片，勾一条只重写当前账号的状态分片）
- 多账号：新建、切换、归档、恢复、删除；账号级数据隔离
- 数据备份：导出为 JSON 文本、粘贴导入（结构校验 + 五项摘要确认 + 覆盖式写入）
- 工具资料三段懒加载：御魂副本轮换与掉落、悬赏封印出处反查、结界寄养 6 小时收续点
- 数据与验收脚本：`tools/build.js`、`tools/calibrate-report.js`、`tools/verify.js`

### 修改

- 重写 `README.md`：补充功能详解、数据规模、口径纪律与常见问题
- `.gitignore` 忽略本机部署脚本 `update.sh`

### 数据

- 条目库数据版本 `2026.09.13-十周年勾玉查漏`：常驻条目 75 条 + 活动期条目 16 条；另含御魂副本 11 个、御魂 70 种、悬赏式神 39 个
