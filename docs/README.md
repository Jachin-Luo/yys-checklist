# 开发者文档索引

> 这套文档写给**接手本项目的开发者与 AI 助手**，内容以源码与配置为唯一真值整理。
>
> 对应数据版本：`2026.09.20-常驻条目按次数拆分` ｜ 事实核对日期：2026-09-20

## 文档地图

| 文档 | 回答什么问题 | 主要读者 |
| --- | --- | --- |
| 根目录 `AGENTS.md` | 有哪些不可违反的红线、常用命令是什么、文档在哪 | **所有人开工前先读这一份** |
| [`01-architecture.md`](./01-architecture.md) | 代码放在哪、谁调用谁、一次点击之后发生了什么 | 需要读懂整体结构与数据流的开发 |
| [`02-data-and-domain.md`](./02-data-and-domain.md) | 数据长什么样、从哪来、规则写在哪个函数 | 改数据、改规则、接后端 |
| [`03-conventions-and-tests.md`](./03-conventions-and-tests.md) | 哪些约束被工具强制、口径纪律落在哪、怎么测、数据怎么录 | 所有提交代码变更的人 |
| [`04-handover-guide.md`](./04-handover-guide.md) | 第一次跑起来做什么、常见改动怎么改、坏了怎么查 | 新接手者、日常迭代者 |
| 根目录 [`CHANGELOG.md`](../CHANGELOG.md) | 改过什么、属于哪个版本、什么时候改的（改动流水唯一入口） | 想知道近期变动的任何人 |

面向玩家的功能说明、玩法口径理由、FAQ、数据说明与免责声明在根目录 [`README.md`](../README.md)，本套文档不重复那部分内容。

## 按角色的推荐阅读路径

| 你的目标 | 读什么 |
| --- | --- |
| 第一次接手，要建立整体认知 | `AGENTS.md` → `01` → `02` → `03` → `04`，边读边跑 `npm run dev` 对照 |
| 只改条目数据 / 升级数据版本 | `AGENTS.md` 的红线第 5、6、7 条 → `02` 第 5、8 节 → `04` 任务 1 |
| 只改界面 / 加页面 | `01` 第 5、9 节 → `03` 第 1 节 → `04` 任务 2、6 |
| 把 Mock 换成真实后端 | `02` 第 1、3 节 → `04` 任务 5 |
| 排查线上问题 | `04` 第 5 节（排查表） |

## 时效与更新约定

1. **锚点是符号名，不是行号。** 文档里引用真实路径与符号名（函数 / store / 常量），行号会随改动漂移，所以只在必要时给出。定位时优先搜符号名。
2. **规模数字要重跑确认。** 文档中的条目数、御魂数、测试用例数等来自核查时的实测；改数据后请重跑 `npm run db:check` 与 `npm test` 再更新文档里的数字。
3. **改了这些，要同步改文档**：

| 改动 | 需要同步的文档位置 |
| --- | --- |
| 条目字段规格 / 枚举 | `02` 第 4、5 节，`03` 第 7 节 |
| 契约新增或删除方法 | `02` 第 1 节，`01` 第 7 节（数据流） |
| 新增 store / 页面 / domain 模块 | `01` 第 2、6、8 节，`02` 第 6 节 |
| 新增目录或改变目录职责 | `01` 第 2 节 |
| 完成路线图中的未完成项 | `04` 第 4 节，必要时更新 `AGENTS.md` 的红线 |
| 新增强制约定（lint 规则、口径纪律） | `AGENTS.md` 红线 + `03` 第 1、2 节 |
| 任何用户可见或结构性改动 | 根目录 `CHANGELOG.md` 的 `[Unreleased]` 区（约定见 `AGENTS.md` 红线第 8 条） |

4. 文档不复制数据明细（例如 103 条条目逐条内容、70 种御魂清单）—— 明细以 `src/db/*.db.json` 为唯一真值，避免双份维护。

## 事实来源

| 结论类型 | 真值来源 |
| --- | --- |
| 目录结构、文件清单 | `src/` 实际文件 |
| 导出符号、签名 | 对应 `.ts` / `.tsx` 源文件 |
| 命令与依赖 | `package.json` |
| 强制约束 | `eslint.config.js`、`tsconfig*.json`、`vite.config.ts` |
| 数据规模与校验规则 | `src/db/*.db.json` 与 `tools/build.js` 的实际行为 |
| 产品口径及其理由 | 根目录 `README.md`（玩家向说明） |
